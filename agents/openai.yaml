interface:
  display_name: "Pi Workflows"
  short_description: "Build and run deterministic Pi workflow graphs"
  default_prompt: "Use $pi-workflows to build and verify a bounded workflow for this task with explicit inputs, typed outputs, mechanical gates, and inspectable run evidence."
