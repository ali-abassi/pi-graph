# Final browser matrix receipt

- States: normal completed, empty/no-runs, degraded/corrupt, interrupted maximum-content.
- Viewports: `1440×900` and `390×844`.
- Horizontal page overflow: `0` in every captured state.
- Browser console errors: `0` in `normal-errors.txt`, `empty-errors.txt`, `degraded-errors.txt`, and `maximum-errors.txt`.
- Maximum state: 200 nodes, 300 edges, 201 committed events, conditional route, produced-file metadata, failed/interrupted nodes, 64 KiB source artifact rendered through an 8,000-character visible bound, three rejected attempts, and copy-only resume command.
- Minimum viewport: closed run rail is inert and `aria-hidden`; selected run status and graph remain available.

Result: `PASS`.
